<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>adoniscasts | JETSTREAM</title>
  <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
  
  <style>
    body {
      font-family: 'Poppins', sans-serif;
    }
  </style>
</head>
<body class="bg-white">
  <header class="text-gray-700 body-font">
    <div class="container mx-auto flex flex-wrap p-5 flex-col md:flex-row items-center">
      <a href="/" class="flex title-font font-medium items-center text-gray-900 mb-4 md:mb-0">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="w-10 h-10 text-white p-2 bg-blue-500 rounded-full" viewBox="0 0 24 24">
          <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"></path>
        </svg>
        <span class="ml-3 text-xl font-black">adoniscasts <span class="font-normal text-blue-500">flare</span></span>
      </a>
      <nav class="md:ml-auto flex flex-wrap items-center text-base justify-center">
        <a href="/login" class="mr-5 hover:text-gray-900">Login</a>
      </nav>
      <a href="/register" class="inline-flex items-center bg-blue-500 border-0 py-2 px-4 focus:outline-none hover:bg-blue-600 rounded-lg text-base mt-4 md:mt-0 text-white">Register
        <svg fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="w-4 h-4 ml-1" viewBox="0 0 24 24">
          <path d="M5 12h14M12 5l7 7-7 7"></path>
        </svg>
      </a>
    </div>
  </header>

  <section class="text-gray-700 body-font">
    <div class="container mx-auto flex px-5 py-24 md:flex-row flex-col items-center">
      <div class="lg:flex-grow md:w-1/2 lg:pr-24 md:pr-16 flex flex-col md:items-start md:text-left mb-16 md:mb-0 items-center text-center">
        <h1 class="title-font sm:text-5xl text-5xl mb-4 font-black text-gray-900">Login
          <br class="hidden lg:inline-block">
        </h1>
        <p class="mb-8 leading-relaxed">Here you can login.</p>
        <div class="flex justify-center">
          
        </div>

        <form class="rounded-lg p-8 bg-gray-100 w-full" action="{{ route('AuthController.login') }}" method="post">
            <div class="pb-4">
              {{--  <label for="email">Email</label>  --}}
              <input class="rounded-lg w-full h-10 p-4" placeholder="Email" type="text" name="email" value="{{ flashMessages.get('email') || '' }}" />
              <p>{{ flashMessages.get('auth.errors.uid') || '' }}</p>
            </div>
        
            <div class="pb-4">
              {{--  <label for="password">Password</label>  --}}
              <input class="rounded-lg w-full h-10 p-4" placeholder="Password" type="password" name="password" />
              <p>{{ flashMessages.get('auth.errors.password') || '' }}</p>
            </div>
        
            <div class="pb-4">
              <button class="bg-blue-500 text-white py-2 px-4 rounded-lg" type="submit">Login</button>
            </div>
          </form>
      </div>
      <div class="lg:max-w-lg lg:w-full md:w-1/2 w-5/6">
      </div>
    </div>
  </section>
</body>
</html>
